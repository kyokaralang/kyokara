import collections

fn main() -> Int {
  let xs = collections.List.new().push(1).push(2)
  let m = collections.Map.new().insert("a", 10)
  let s = collections.Set.new().insert(1).insert(1)
  xs.len() + m.len() + s.len()
}
