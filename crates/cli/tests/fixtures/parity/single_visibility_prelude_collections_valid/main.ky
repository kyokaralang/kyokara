import collections

fn main() -> Int {
  let xs = collections.MutableList.new().push(1).push(2).to_list()
  let m = collections.MutableMap.new().insert("a", 10).to_map()
  let s = collections.MutableSet.new().insert(1).insert(1).to_set()
  xs.len() + m.len() + s.len()
}
