//! `xtask` — Build automation for the Kyokara project.
//!
//! Run with `cargo xtask <command>`.
//!
//! Planned tasks:
//! - `codegen` — Generate boilerplate for typed AST node wrappers.

fn main() {
    eprintln!("xtask: no tasks defined yet");
}
